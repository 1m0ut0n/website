---
import Link from "@components/Link.astro";
import { SITE } from "@consts";
---

<header>
  <div class="mx-auto py-5 px-4 [html.scrolled_&]:pt-2.5 [html.scrolled_&]:pb-3 [html.scrolled_&]:px-5 transition-all duration-700 ease-in-ou max-w-screen-sm backdrop-blur-md saturate-200 bg-gradient-to-b from-neutral-100 bg-opacity-75 dark:from-neutral-900 dark:bg-opacity-75 from-10% rounded-b-lg border-b-2 sm:border-x-2 border-transparent [html.scrolled_&]:border-black/15 [html.scrolled_&]:dark:border-white/20">
      <div class="flex flex-wrap gap-y-2 justify-between">
        <Link href="/" underline={false}>
          <div class="font-semibold">
            {SITE.NAME}.net
          </div>
        </Link>
        <nav class="flex gap-6">
          <Link href="/blog">
            <span class="hidden sm:inline">The </span>
            <span>Blog</span>
          </Link>
          <Link href="/work">
             <span class="hidden sm:inline">The </span>
            <span>Work</span>
          </Link>
          <Link href="/projects">
            <span class="hidden sm:inline">The </span>
            <span>Projects</span>
          </Link>
        </nav>
      </div>
    </div>
</header>
